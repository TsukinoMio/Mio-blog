# 配置速查表

> **这份文档只回答一个问题：我想改 XX，该去哪个文件。**
>
> 具体怎么改（写文章的 frontmatter 格式、换背景的步骤、换音乐的字段说明、
> 搜索和目录怎么工作……）统一写在项目根目录的 **`README.md`** 里，
> 这里不重复，免得两边改得不一致。
>
> 项目路径：`C:\Users\Hikami\Desktop\personalWeb`

---

## 一、五个配置 / 数据文件，谁管什么

| 文件 | 管什么 | 改完要不要重启 dev server |
| --- | --- | --- |
| `src/config/site.ts` | 站点"身份"：名字、标语、定位、导航、社交链接、域名、播放器行为 | 需要（`.ts` 文件） |
| `src/config/theme.ts` | 背景观感（透明度/模糊/遮罩）、氛围特效开关 | 需要 |
| `src/config/copy.ts` | 界面上所有按钮 / 标题 / 提示文字（"UI 外壳"文案） | 需要 |
| `src/data/profile.json` | 关于页的头像路径与自我介绍段落 | 不需要（JSON 热更新） |

**另外两个 JSON 是自动生成的，不要手改**（改了下次构建就被覆盖）：

| 文件 | 由什么生成 |
| --- | --- |
| `src/data/music.json` | 扫描 `public/audio/*.mp3` 的 ID3 标签 |
| `src/data/backgrounds.json` | 扫描 `public/images/backgrounds/` |

生成器是 `scripts/sync-media.mjs`，挂在 `predev` / `prebuild` 上自动跑；
想手动跑一次：`npm run sync:media`。

**一句话该改哪个文件，按这个判断：**

- 涉及"我是谁"（名字、简介、社交账号）→ `site.ts`
- 是按钮 / 标题 / 空状态这类界面装饰文字，换个说法意思不变 → `copy.ts`
- **换歌 / 换背景 → 不用改文件，直接把 mp3 或图片丢进对应目录**
- 是文章内容、个人经历 → `content/blog/*.mdx` 或 `data/profile.json`

---

## 二、快速对照表：我想改 XX，去哪个文件

| 我想改… | 去这里 |
| --- | --- |
| 站名 / 首页大字标语 / 个人定位 | `src/config/site.ts` |
| 浏览器标签页的小图标（favicon） | 图放 `public/images/`，路径写进 `src/config/site.ts` 的 `icon`。**别放 `public/` 根目录**（缓存规则盖不到），也**别放回 `src/app/icon.*`**（文件约定会盖掉配置） |
| 标签页标题里名字后面那个 `·` | 它来自 `site.ts` 的 `tagline`；`tagline` 留空就不会出现分隔符 |
| 顶部导航菜单项 | `src/config/site.ts` 的 `nav` |
| B 站 / X / GitHub / Email 链接 | `src/config/site.ts` 的 `social` |
| 自动播放开关、随机还是固定曲目、初始音量 | `src/config/site.ts` 的 `player` |
| 线上域名 | 环境变量 `NEXT_PUBLIC_SITE_URL`（见 `.env.example`），不用改代码 |
| "最新文章""全部文章"这类按钮 / 标题文字 | `src/config/copy.ts` |
| 播放器里"播放""静音""单曲循环"这些字 | `src/config/copy.ts` 的 `music` 分组 |
| 搜索框占位提示、无结果文案 | `src/config/copy.ts` 的 `search` 分组 |
| 文章目录面板的"目录"标题 | `src/config/copy.ts` 的 `toc` 分组 |
| 404 页文案 | `src/config/copy.ts` 的 `notFound` 分组 |
| 背景图 | **只需把图片放进 `public/images/backgrounds/`**，自动扫描，不用改配置 |
| 背景的透明度 / 模糊 / 遮罩 / 是否固定 | `src/config/theme.ts` 的 `background` |
| 星光 / 光晕开关 | `src/config/theme.ts` 的 `effects` |
| 头像、自我介绍段落 | `src/data/profile.json` |
| 歌单 | **只需把 mp3 放进 `public/audio/`**，曲名/艺术家/专辑/时长/封面全从 ID3 自动读，不用改配置 |
| 文章 | `content/blog/*.mdx`，新建文件即可 |
| 搜索结果里一篇文章折叠时先显示几条 | `src/lib/search.ts` 的 `PREVIEW_SNIPPETS_PER_DOC`（默认 1，其余点「还有 N 处结果」展开） |
| 一篇文章最多整理几处搜索命中 | `src/lib/search.ts` 的 `MAX_SNIPPETS_PER_DOC`（默认 50，防止高频词甩出几百个节点） |
| 搜索的模糊程度 / 字段权重 | `src/components/layout/SearchBox.tsx` 的 `FUSE_OPTIONS` |
| 默认主题色 / 调色滑块的取色算法 | `src/app/globals.css` 的 `:root` + `src/providers/ThemeProvider.tsx` |

---

## 三、`copy.ts` 的两种字段

打开 `src/config/copy.ts`，用 Ctrl+F 搜要改的中文原文（比如"最新文章"）就能定位。里面有两种字段：

- **普通字符串**：`viewAllPosts: '全部文章'` —— 直接改引号里的字
- **函数**：`readingTime: (minutes) => \`${minutes} 分钟\`` —— 文案里嵌了动态数字。
  改前后的文字就行，**别删掉 `${minutes}`**，删了数字就不显示了

分组一览：`common` / `header` / `search` / `home` / `blog` / `about` / `music` / `toc` / `theme` / `notFound`

---

## 四、主题强调色系统（进阶，想改配色逻辑时看）

首页左侧那个色相滑块背后的机制，和上面的文案配置是两套独立系统，容易搞混：

1. `src/app/globals.css` 里定义了一组 CSS 变量：`--accent-1/2/3`（渐变色标）、
   `--accent-strong-1/2`（按钮用的深色版）、`--accent-foreground`（按钮上文字的颜色）、
   `--accent-shadow`（辉光）、`--accent-border`（卡片悬停描边）。默认全是白色系。
2. 全站原本写死"粉色渐变"的地方（按钮、头像光环、卡片辉光、进度条）现在都引用这些变量，
   而不是直接写 `sakura-500` 这种固定颜色。
3. `src/providers/ThemeProvider.tsx` 里的滑块是 0~100：拖到 0 是纯白，
   其他值用 HSL 公式现算出一套颜色，直接写成 `<html>` 标签的内联样式
   （内联样式优先级天然盖过 CSS 里的默认值），页面上所有引用这些变量的地方就瞬间跟着变。
4. 选中的颜色存在浏览器 `localStorage`（key 是 `theme-hue`），刷新还在。

**想改默认主题色** → 改 `globals.css` 里 `:root { --accent-1: #ffffff; ... }` 那一块。
**想换取色风格** → 改 `ThemeProvider.tsx` 的 `applyHue` 函数，里面是标准 HSL 转十六进制，
调饱和度 / 明度参数就能整体改调性。

> ⚠️ 以后新写界面时，凡是"强调色"性质的渐变或辉光，请用 `bg-accent-gradient`、
> `shadow-glow-accent` 这类工具类，**不要写死 `from-sakura-500 to-lavender-500`**，
> 否则那块颜色不会跟着滑块变。

---

## 五、有哪些东西是全自动的，不用配置

- **文章目录**：构建期从正文的 `#` / `##` / `###` 自动抽取
- **搜索索引**：构建期从全部文章自动生成，新增文章重新构建就自动收录
- **sitemap / RSS 式的站点地图**：随文章自动更新
- **阅读时长**：按中文 400 字/分、英文 200 词/分自动估算
- **歌单与专辑封面**：构建前自动扫描 `public/audio/`，从 mp3 的 ID3 标签读曲名/艺术家/专辑/时长，并把内嵌封面抽成图片文件
- **背景图列表**：构建前自动扫描 `public/images/backgrounds/`

细节都写在 `README.md` 里。
