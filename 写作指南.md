# 写作指南：写文章 · Obsidian · 图床 · 换背景换音乐

> **四份文档的分工**（内容不重复，改哪份都不用同步其他几份）：
> - **本文件** —— 内容怎么生产：写文章、Obsidian 工作流、图床、换背景、换音乐
> - **`配置与部署.md`** —— 我想改 XX 去哪个文件 + 改完怎么让线上生效 + 出问题怎么查
> - **`README.md`** —— 项目入口与目录结构
> - **`AI_CONTEXT.md`** —— 给 AI 助手看的项目交接文档

---

## 目录

- [一、最短路径：发一篇文章](#一最短路径发一篇文章)
- [二、frontmatter 完整说明](#二frontmatter-完整说明)
- [三、cover 与图床（最容易出错的地方）](#三cover-与图床最容易出错的地方)
- [四、Obsidian 工作流](#四obsidian-工作流)
- [五、正文能写什么](#五正文能写什么)
- [六、代码块语言白名单（不报错但会失效）](#六代码块语言白名单不报错但会失效)
- [七、从语雀 / Notion / 飞书导入](#七从语雀--notion--飞书导入)
- [八、文章目录是自动的](#八文章目录是自动的)
- [九、搜索是自动的](#九搜索是自动的)
- [十、换背景图](#十换背景图)
- [十一、换音乐](#十一换音乐)
- [十二、改关于页](#十二改关于页)

---

## 一、最短路径：发一篇文章

```bash
# 1. 在 content/blog/ 下新建 my-post.md
#    文件名就是网址：my-post.md -> /blog/my-post

# 2. 填好 frontmatter（见第二节），写正文

# 3. 本地自查 —— 这一步别省，很多错误只在构建期暴露
npm run build

# 4. 提交推送，Cloudflare 自动构建约 3 分钟
git add content/blog/my-post.md
git commit -m "post: 文章标题"
git push origin main
```

列表页、分类筛选、标签、文章目录、搜索索引、sitemap、相关文章、上下篇导航
**全部自动更新**，不需要任何额外配置。

> `.md` 和 `.mdx` 两种后缀都支持（`posts.ts` 的过滤是 `/\.mdx?$/`）。
> Obsidian 只认 `.md`，所以现在统一用 `.md`。

---

## 二、frontmatter 完整说明

frontmatter 是文件最开头、两条 `---` 之间的那一段。**它不是正文**，是这篇文章的档案卡：
网站靠它生成列表卡片、分类筛选按钮、搜索索引和 SEO 信息。

```markdown
---
title: 基于物理的渲染（PBR）学习笔记
date: 2026-08-06
summary: 整理 PBR 渲染管线里绕不开的几个概念，附一份 Unity HLSL 实现。
category: 图形学
tags:
  - 图形学
  - PBR
cover: "https://img.reikaakane.com/file/blog/xxxx.png"
draft: false
---

正文从这里开始。
```

| 字段 | 必填 | 作用 | 写错的后果 |
|---|---|---|---|
| `title` | ✅ | 卡片大字 / 浏览器标签 / 搜索结果 | 构建失败 |
| `date` | ✅ | 发布日期，决定排序 | 构建失败 |
| `summary` | ✅ | 卡片小字 / SEO 描述 / 分享卡片描述 | 构建失败 |
| `category` | ✅ | 分类，一篇只能一个，会变成 `/blog` 的筛选按钮 | 构建失败 |
| `tags` | 可选 | 标签，可多个。也是「相关文章」的匹配依据 | 默认空数组 |
| `cover` | 可选 | 封面图。**见第三节，最容易写错** | 不填会显示渐变占位 |
| `draft` | 可选 | `true` = 不发布。线上跳过，`npm run dev` 里仍能看到 | 默认 `false` |
| `slug` | 可选 | 自定义网址 | 不填就用文件名 |

校验规则写在 `src/lib/schema.ts`（用 zod），**只在构建期检查**。

### ⚠️ 字段不能留空

```markdown
title:          ← 冒号后面什么都不写 = 空值，不是空字符串
```

空值过不了校验，`npm run build` 直接中断：

```
Error: 文章 content/blog/test01.md 的 frontmatter 有误：
  - title: Invalid input: expected string, received null
```

**`draft: true` 救不了这个** —— 校验发生在草稿过滤之前，字段该合法还得合法。
写到一半的稿子有两个选择：填上占位值 + `draft: true`，或者暂时挪出 `content/blog/`。

> Obsidian 的模板（`MioSrc/blogTemplate/模板.md`）已经预填了占位值和 `draft: true`，
> 用模板新建就不会踩这个坑。

---

## 三、cover 与图床（最容易出错的地方）

### 规则：`cover` 必须是纯地址

```yaml
✅  cover: "https://img.reikaakane.com/file/blog/xxxx.png"     图床外链
✅  cover: "/images/blog/my-post/01.png"                        站内本地图
❌  cover: "![xxxx.png](https://img.reikaakane.com/....png)"    Markdown 语法
```

**正文里插图正常用 `![](地址)`**，那里就该是 Markdown 语法。只有 `cover` 这个字段要纯地址。

### 写成 Markdown 语法会发生什么（2026-08-09 实际踩过）

从 Obsidian 复制图片时很容易把 `![]()` 那层壳一起粘进 `cover`。后果是三重的，而且**极具迷惑性**：

1. **本地 `npm run dev` 下整站 500** —— 不只是这篇文章，**首页也挂**，因为首页的文章卡片要渲染这个封面
   ```
   Error: Failed to parse src "![xxxx.png](https://img...)" on `next/image`
   GET / 500 · GET /blog/xxx 500
   ```
2. **但 `npm run build` 会通过**（实测退出码 0、零 error 输出），产出一个废地址：
   ```
   /_next/image?url=![xxxx.png](https://img.reikaakane.com/...)&w=3840
   ```
   **构建能过反而更危险** —— CI 会部署成功，线上图全裂，你不主动看是发现不了的。
3. **分享卡片和搜索引擎信息一起损坏**：`og:image`、`twitter:image`、JSON-LD 的 `image`
   全变成两个地址粘在一起的乱码。

### 一张图是怎么到访客屏幕上的

访客的浏览器**不直接去图床拿图**，中间隔了一层优化：

```
访客打开文章
  ↓
页面里的 <img> 指向的是你自己站点的地址：
  /_next/image?url=https://img.reikaakane.com/...&w=1080&q=75
  ↓
你的网站去图床下载原图 → 缩放 / 转格式 → 把处理过的图发给访客
```

实测收益（test01 那张封面，384×640）：

| | 体积 |
|---|---|
| 图床原图 PNG | 465 KB |
| 线上优化后 AVIF | **34.5 KB** |
| 压缩率 | **92.6%** |

它还会按访客屏幕宽度自动挑档位（640 / 750 / 828 / 1080 / 1200 / 1920 / 2048 / 3840 共 8 档），
手机不会去下 4K 大图。**图比原图窄时不会放大**，所有档位落回原始尺寸，这是正确行为。

### 换新图床域名要改代码

`next.config.ts` 里有一份白名单：

```ts
images: {
  remotePatterns: [
    { protocol: 'https', hostname: 'img.reikaakane.com', pathname: '/file/**' },
  ],
}
```

不在白名单里的域名，`/_next/image` 直接返回 **400，图全裂**，而且**构建期毫无征兆**。

为什么要有白名单：不限制的话，任何人都能构造
`https://reikaakane.com/_next/image?url=https://某个巨大的文件` ——
你的服务器就替他去下载、去处理，白送流量和算力。
限定到 `pathname: '/file/**'` 是同样的道理：将来图床上挂了别的服务也不会顺带被当成图片代理。

### 什么情况要改代码，什么情况不用

`pathname: '/file/**'` 里的 `**` 是**任意深度通配**，`/file/` 后面爱建几层建几层。

| 场景 | 要不要改代码 |
|---|---|
| 插件里把 `uploadFolder` 从 `blog` 改成别的 | ❌ 不用 |
| 图床里手动新建子文件夹、手动传图 | ❌ 不用 |
| 目录嵌很多层，如 `/file/a/b/c/x.png` | ❌ 不用 |
| 换图床程序，URL 不再以 `/file/` 开头 | ✅ 要改 |
| 换域名，如 `cdn.reikaakane.com` | ✅ 要改 |

图床当前的地址结构是 `https://img.reikaakane.com/file/<uploadFolder>/<文件名>`，
`/file/` 是图床程序固定的前缀，`<uploadFolder>` 才是插件里那个可配置项（当前是 `blog`）。

### 图裂时怎么区分是哪种问题

直接请求优化接口，看返回的错误：

```bash
curl -s --noproxy '*' "https://reikaakane.com/_next/image?url=<图片地址做URL编码>&w=640&q=75"
```

| 返回 | 含义 | 怎么办 |
|---|---|---|
| `200` | 正常 | —— |
| **`400`** `"url" parameter is not allowed` | **白名单拒绝** | 改 `remotePatterns` |
| **`404`** `"url" parameter is valid but upstream response is invalid` | **白名单通过了**，是图床上没这个文件 | 检查图片路径/是否传成功 |

**换域名或换图床程序时找 AI 加一行即可。**

### 本地图 vs 图床，怎么选

| | 放哪 | 适用 |
|---|---|---|
| **文章配图** | 图床 `img.reikaakane.com` | 数量会越来越多，放仓库会撑爆体积 |
| **背景图** | 本地 `public/images/backgrounds/` | **刻意不挪图床**，理由见 `AI_CONTEXT.md` 的 ADR-10 |

本地配图放 `public/images/blog/<文章slug>/`，正文里写 `/images/blog/<文章slug>/xxx.png`。
两种混着用没问题，代码会自动判断（`lib/utils.ts` 的 `toAbsoluteUrl`）。

---

## 四、Obsidian 工作流

项目根目录本身就是 Obsidian 库。装了三个插件：

| 插件 | 用途 |
|---|---|
| `astro-composer-hanhua` | 按模板新建文章 |
| `cf-imageBed` | 截图直接上传图床，正文里自动插外链 |
| `obsidian-git` | 在 Obsidian 里直接提交推送 |

**模板在 `MioSrc/blogTemplate/模板.md`**，已经预填占位值 + `draft: true` + 一段注释说明，
新建笔记时照着填即可，写到一半也不会卡住构建。

### 几个约定

- 文章后缀用 **`.md`**（Obsidian 只认这个）
- 写完把 `draft` 改成 `false`
- 推送前跑一次 `npm run build` 自查
- **`.obsidian/` 和 `MioSrc/` 都在 `.gitignore` 里**，不会进仓库

### ⚠️ `.obsidian/` 为什么必须忽略

`.obsidian/plugins/cf-imageBed/data.json` 里存着图床的 `authCode`，
而**这个仓库是公开的**。一旦提交，任何人都能拿到那串凭据往你的图床传东西。
另外插件 JS 和主题 CSS 加起来超过 2.5 MB，进了 git 就永久留在历史里删不掉。

**别把 `.obsidian/` 加回仓库。** 代价是换机器要重新配一次 Obsidian 库，值得。

---

## 五、正文能写什么

支持全部标准 Markdown，外加下面几样。

### 提示块

```markdown
<Note type="tip">这里是一段提示。type 可选 info / tip / warn。</Note>
```

### 数学公式（KaTeX）

```markdown
行内公式 $E = mc^2$，或者独占一行的块级公式：

$$
L_o(p, \omega_o) = \int_{\Omega} f_r(p, \omega_i, \omega_o) L_i(p, \omega_i) \max(0, \mathbf{n} \cdot \omega_i) d\omega_i
$$
```

**判断标准**：嵌在句子中间的用 `$...$`；自己单独占一段的用 `$$...$$`，前后各留一个空行。
用错了不会报错，但块级公式写成行内会字很小、不居中。

### 代码块（可带文件名）

````markdown
```tsx title="src/app/page.tsx"
export default function Page() {}
```
````

---

## 六、代码块语言白名单（不报错但会失效）

**这是最容易踩、而且完全不会报错的坑。**

`src/lib/shiki.ts` 里只注册了三种语言：

```ts
const LANGS = [glsl, tsx, bash];
```

用了清单外的语言（比如 ` ```python `），**构建不会失败，但那段代码不会有配色**，
当成纯文本渲染，也没有任何警告。

### 为什么要有白名单

Shiki 的完整包含 200 多种语言语法，打进 Worker 后 gzip 约 1.74 MB。
Cloudflare Workers 免费版脚本上限是 **3 MiB（gzip 后）**，带上完整包直接超限：

```
Your Worker exceeded the size limit of 3 MiB. [code: 10027]
```

只保留三种后，同一条依赖链从 10.15 MB / gzip 1.74 MB 降到 0.61 MB / gzip 0.11 MB。

### 加一种新语言

假设要写 Python，改 `src/lib/shiki.ts` 两行：

```ts
import bash from '@shikijs/langs/bash';
import glsl from '@shikijs/langs/glsl';
import python from '@shikijs/langs/python';   // ← 1. 加这行
import tsx from '@shikijs/langs/tsx';

const LANGS = [glsl, tsx, bash, python];      // ← 2. 加进数组
```

依赖语法会自动跟着走，不用手动补。改完 `npm run build`，打开对应文章确认代码块有颜色。

> 常用包名：`python` `json` `yaml` `sql` `rust` `go` `cpp` `csharp` `java` `css` `html`
> `markdown` `typescript` `javascript` `shellscript`。
> 完整列表见 `node_modules/@shikijs/langs/dist/` 下的文件名。

---

## 七、从语雀 / Notion / 飞书导入

导出的 `.md` 直接拖进 `content/blog/` 大概率不能用，五个常见坑：

1. **没有 frontmatter** —— 补上必填字段（见第二节），否则构建期报错。
2. **正文被 `<font style="color:...">` 包裹** —— 很多导出工具会给每段文字包一层内联颜色。
   那是它们编辑器里的配色，跟本站主题无关。用正则替换把 `<font[^>]*>` 和 `</font>`
   都换成空字符串，再检查有没有把加粗 `**text**` 拆散。
3. **数学公式只用单个 `$`** —— 独立成段的公式要改成 `$$...$$`，判断标准见第五节。
4. **图片是外链到人家的 CDN**（语雀是 `cdn.nlark.com`）—— 这些链接长期可能失效或需要登录。
   要么下载下来放 `public/images/blog/<slug>/`，要么重新传一遍到自己的图床。
5. **代码块语言不在白名单** —— 见第六节。

`content/blog/physically-based-rendering.md` 就是从语雀导出后原样整合的一篇图形学笔记，
可以对照参考。

---

## 八、文章目录是自动的

不用手写。构建时从正文扫出所有 `#` / `##` / `###`，文章页右侧就出现目录面板
（标题少于 2 个时不显示）。滚动会自动高亮当前章节。

- **位置**：宽屏（≥1280px）贴着正文右边缘常驻展开；窄屏退回成圆按钮，点开才显示
- **折叠**：有子标题的章节前面带箭头，点一下收起/展开这一支。当前小节被折叠藏起来时，
  高亮会自动回退到那个折叠的上级，这样始终知道自己在哪一大节
- 只收录到三级标题，`####` 及以下不进目录
- 有的文章用 `#` 当章节标题、有的从 `##` 开始，目录会按这篇文章里最浅的那一级对齐
- 锚点用 `github-slugger`，和正文标题 id 的生成规则是同一个库，
  中文标题、带引号或行内代码的标题都能正确跳转

实现在 `src/lib/toc.ts` + `src/components/blog/ArticleToc.tsx`。

---

## 九、搜索是自动的

**加了新文章不用做任何事。** 索引由 `src/app/search-index.json/route.ts` 在构建期
从 `lib/posts.ts` 生成，输出成静态的 `/search-index.json`。丢新文章、重新构建，自动收录。

搜索范围覆盖**全部文章的标题、摘要、标签、分类和正文全文**，支持模糊匹配。

结果按文章分组，标题右边标着这篇里命中几处，下面按**正文小节**分组显示。
一篇默认先显示 1 条，其余收在「在这篇文章里还有 N 处结果」后面，点一下就地展开。
点任意一条跳进文章并**直接滚到那一句**，页面上所有出现该关键词的地方都会标黄。

几个取舍：

- **为什么用 Fuse.js 而不是 MiniSearch / Lunr**：后者基于分词器按空格切词，
  中文句子没有空格会被当成一整个词，搜索直接失效。Fuse 在原字符串上做匹配，中英文都能用
- **索引什么时候下载**：第一次点开搜索框才 `fetch`，之后整页生命周期复用，首屏不受影响
- **正文会先洗成纯文本**，去掉代码块、公式、图片链接和 Markdown 语法，
  这样结果里显示的句子读起来是人话
- **索引和页面扫描都跳过代码块和公式**，否则「第 4 处」在两边指的不是同一个位置
- **至少要输 2 个字**，单字符匹配不出有意义的结果

调阈值和条数上限的位置见 `配置与部署.md`。

---

## 十、换背景图

**把图片丢进 `public/images/backgrounds/` 就完事了，不用改任何配置。**

由 `scripts/sync-media.mjs` 自动扫描，产出 `src/data/backgrounds.json`（**生成物，别手改**）。
支持 jpg / jpeg / png / webp / avif / gif。

**丢进去的图会被自动转成 WebP**（jpg / jpeg / png / bmp / tif 都会转），
同时把宽度限制在 3840px 以内。原图不会被销毁，会挪到 `MioSrc/media-originals/`
留底 —— 那个目录不进仓库，确认没问题后随时可以自己清空。

实测（本站这四张）：

| 原图 | 转换后 | 省 |
|---|---|---|
| `manaka.png` 21.71 MB（4995×2838） | 1.35 MB | 94% |
| `kotone.png` 12.56 MB（7680×4320） | 0.22 MB | 98% |
| `amami_haruka.png` 8.38 MB | 1.19 MB | 86% |
| `mirai_shizuka_tsubasa.png` 3.92 MB | 0.49 MB | 87% |
| **合计 46.57 MB** | **3.25 MB** | **93%** |

**为什么值得做**：这些图会永久留在 git 历史里，46 MB 进去就再也拿不出来了
（仓库已经因为反复替换音频吃过一次亏）。访客那边其实感觉不到差别 ——
他们拿到的是 `next/image` 现场生成的 AVIF，和源文件大小无关（实测这四张
输出 38~177 KB）。**所以这一步省的是仓库，不是流量。**

每次进入页面从候选里随机挑一张。目录里**只放一张**就等于固定背景；**清空目录**则只保留渐变。

观感参数（透明度 / 模糊 / 是否固定 / 遮罩）和氛围特效开关在 `src/config/theme.ts`，
具体字段见 `配置与部署.md`。

星光与光晕都是**纯 CSS**，没有 Canvas / WebGL，并且会跟随主题强调色。

---

## 十一、换音乐

**把 mp3 丢进 `public/audio/` 就完事了，不用改任何配置。**

曲名、艺术家、专辑、时长、封面全部从 mp3 自带的 ID3 标签读。
封面会被自动抽成图片文件存到 `public/images/covers/`，页面上显示的就是它。

**码率高于 128k 的会被自动压到 128k，不用自己先压一遍。**
内嵌封面和 ID3 标签原样保留，原文件挪到 `MioSrc/media-originals/` 留底
（那个目录不进仓库，随时可以自己清空）。实测：

```
灯里爱夏 - ともすれば、（中略）アイドル.mp3
  320k -> 128k，12.98 MB -> 5.28 MB
  封面流 mjpeg 500x500 保住，标题/艺术家/专辑/时长均无损失
```

**已经是 128k 的不会被重压。** 判断阈值是 138k，留了 8% 余量 ——
mp3 是有损格式，重复转码只会一次次掉音质，而 VBR 文件的实测码率常在
标称值上下浮动，卡死 128 会让它们每次构建都被重压一遍。

由 `scripts/sync-media.mjs` 完成，挂在 `predev` / `prebuild` 上自动跑。手动跑一次：

```bash
npm run sync:media
```

几个注意点：

- **`src/data/music.json` 是生成的，别手改** —— 下次构建会被覆盖
- **mp3 最好带内嵌封面**。没有不会报错，播放器显示渐变占位块，脚本也会提示有几首缺封面
- 曲目顺序按**文件名**排序。想指定顺序就加前缀，比如 `01 - xxx.mp3`
- 曲目 id 由 ID3 标题生成，两首同名会自动加 `-2` 后缀，不会撞车
- 纯中日文标题的曲目 slug 会被清空，id 退回 `track-N`（按序号），这是正常的
- **自己手动转码时别用 ffmpeg 的 `-vn`**，那会把内嵌封面剥掉。要用 `-map 0 -c:v copy` 带上封面流（脚本里已经这么做了）

### ⚠️ 自动优化依赖本机的 ffmpeg

音频压缩和图片转 WebP 都要调 ffmpeg。**Cloudflare 的构建环境里没有 ffmpeg**，
所以脚本检测不到就静默跳过，不会让构建失败。

这意味着：**丢完文件要在本地跑一次再提交**，否则进仓库的是没优化过的原文件。

跑 `npm run dev` 就会自动带上（`predev` 钩子），或者手动：

```bash
npm run sync:media
```

脚本是**幂等**的，重复跑不会重复处理 —— 已经是 128k 的音频和已经是 `.webp`
的图片都会被直接跳过。

想把整批音频搬到 CDN，设一个环境变量即可，所有相对路径会自动加前缀：

```bash
NEXT_PUBLIC_AUDIO_BASE_URL=https://cdn.example.com
```

### 播放器的行为

音乐**没有独立页面**，是常驻左下角的浮动播放条。封面、进度、音量、
上一首/播放/下一首/循环模式都在条上，点封面或箭头展开歌单。

`<audio>` 只有一个，挂在根布局里。切换页面时根布局不卸载，所以**音乐不会中断**。

封面唱片的旋转：暂停时**冻结在当前角度**而不是弹回水平，恢复播放从原地继续；
收起态的小圆钮和展开态的封面是两个不同 DOM 节点，但共用同一份累计旋转时长，
切换时角度完全对得上。

### 自动播放

开关在 `src/config/site.ts` 的 `player` 段，字段说明见 `配置与部署.md`。

**必须知道的浏览器限制**：Chrome / Safari / Firefox 都默认**禁止带声音的自动播放**，
除非用户之前跟这个网站交互过。这不是代码能绕过去的，任何声称能强行自动播放的方案
要么是静音播放、要么早就失效了。

所以实现是两段式的：进页面先尝试播放，被拦下来就静默等着，
在用户第一次点击 / 按键 / 触摸屏幕时立刻接上，全程不弹提示。
你自己常来基本每次都能直接响；别人第一次访问，会在他点页面任何地方的瞬间响起来。

---

## 十二、改关于页

`src/data/profile.json` —— 头像路径与自我介绍段落。
头像换成自己的照片：放进 `public/images/`，改 JSON 里的 `avatar` 字段。

社交链接卡片读的是 `src/config/site.ts` 的 `social` 数组。
目前只有 B 站是真的，X / GitHub / Email 在文件里注释着，拿到真实地址取消注释即可。

想加新平台：在 `src/components/about/SocialIcons.tsx` 加图标，
再去 `site.ts` 的 `SocialPlatform` 类型里加一个值。
