# 配置与部署：改哪个文件 · 怎么上线 · 出问题怎么查

> **四份文档的分工**（内容不重复，改哪份都不用同步其他几份）：
> - **本文件** —— 我想改 XX 去哪个文件 + 改完怎么让线上生效 + 出问题怎么查
> - **`写作指南.md`** —— 内容怎么生产：写文章、Obsidian、图床、换背景换音乐
> - **`README.md`** —— 项目入口与目录结构
> - **`AI_CONTEXT.md`** —— 给 AI 助手看的项目交接文档

---

## 目录

**第一部分 · 配置**
- [1. 我想改 XX，去哪个文件](#1-我想改-xx去哪个文件)
- [2. 配置文件各管什么](#2-配置文件各管什么)
- [3. `copy.ts` 的两种字段](#3-copyts-的两种字段)
- [4. 主题强调色系统（进阶）](#4-主题强调色系统进阶)
- [5. 哪些东西是全自动的，不用配置](#5-哪些东西是全自动的不用配置)

**第二部分 · 发布**
- [6. 发布流程](#6-发布流程)
- [7. Cloudflare 部署配置一览](#7-cloudflare-部署配置一览)
- [8. 换域名的完整清单](#8-换域名的完整清单)

**第三部分 · 排查**
- [9. 出问题怎么查](#9-出问题怎么查)
- [10. 上线后自检](#10-上线后自检)
- [11. 回滚](#11-回滚)
- [12. 本地预览 Cloudflare 产物（可选）](#12-本地预览-cloudflare-产物可选)

---

# 第一部分 · 配置

## 1. 我想改 XX，去哪个文件

| 我想改… | 去这里 |
| --- | --- |
| 站名 / 首页大字标语 / 个人定位 | `src/config/site.ts` |
| 浏览器标签页图标（favicon） | 图放 `public/images/`，路径写进 `site.ts` 的 `icon`。见下方注意事项 |
| 标签页标题里名字后面那个 `·` | 来自 `site.ts` 的 `tagline`，留空就不会出现分隔符 |
| 顶部导航菜单项 | `site.ts` 的 `nav` |
| B 站 / X / GitHub / Email 链接 | `site.ts` 的 `social` |
| 自动播放开关 / 随机还是固定曲目 / 初始音量 | `site.ts` 的 `player` |
| 线上域名 | 环境变量 `NEXT_PUBLIC_SITE_URL`，**不用改代码**，见第 8 节 |
| 「最新文章」「全部文章」这类按钮和标题文字 | `src/config/copy.ts` |
| 播放器里「播放」「静音」「单曲循环」这些字 | `copy.ts` 的 `music` 分组 |
| 搜索框占位提示 / 无结果文案 | `copy.ts` 的 `search` 分组 |
| 文章目录面板的「目录」标题 | `copy.ts` 的 `toc` 分组 |
| 404 页文案 | `copy.ts` 的 `notFound` 分组 |
| 背景的透明度 / 模糊 / 遮罩 / 是否固定 | `src/config/theme.ts` 的 `background` |
| 星光 / 光晕开关 | `theme.ts` 的 `effects` |
| 头像、自我介绍段落 | `src/data/profile.json` |
| 默认主题色 / 调色滑块的取色算法 | `src/app/globals.css` 的 `:root` + `src/providers/ThemeProvider.tsx` |
| 搜索结果里一篇文章折叠时先显示几条 | `src/lib/search.ts` 的 `PREVIEW_SNIPPETS_PER_DOC`（默认 1） |
| 一篇文章最多整理几处搜索命中 | `src/lib/search.ts` 的 `MAX_SNIPPETS_PER_DOC`（默认 50，防止高频词甩出几百个节点） |
| 搜索的模糊程度 / 字段权重 | `src/components/layout/SearchBox.tsx` 的 `FUSE_OPTIONS` |
| 代码块支持的语言 | `src/lib/shiki.ts` 的 `LANGS`，见 `写作指南.md` 第六节 |
| 图床 / 外部图片的域名白名单 | `next.config.ts` 的 `images.remotePatterns`，见 `写作指南.md` 第三节 |
| **背景图** | **不用改文件**，图片丢进 `public/images/backgrounds/` |
| **歌单** | **不用改文件**，mp3 丢进 `public/audio/` |
| **文章** | `content/blog/*.md`，新建文件即可 |

### favicon 的三个注意事项

```ts
icon: '/images/my-icon.png',
```

- **放 `public/images/` 而不是 `public/` 根目录** —— `public/_headers` 给 `/images/*`
  配了 30 天缓存，放根目录那条通配盖不到，图标每次导航都要回源校验一次
- **别把图标放回 `src/app/icon.*`** —— 那是 Next.js 的文件约定，只要那个文件存在
  就会自动接管并覆盖 `site.ts` 的配置。项目自带的默认图标在 `public/images/icon.svg`
- 做成**正方形**（512×512 或 180×180），浏览器会把它塞进方形槽位，非正方形会被拉伸变形

---

## 2. 配置文件各管什么

| 文件 | 管什么 | 改完要不要重启 dev server |
| --- | --- | --- |
| `src/config/site.ts` | 站点「身份」：名字、标语、定位、导航、社交链接、域名、播放器行为 | 需要（`.ts` 文件） |
| `src/config/theme.ts` | 背景观感（透明度/模糊/遮罩）、氛围特效开关 | 需要 |
| `src/config/copy.ts` | 界面上所有按钮 / 标题 / 提示文字 | 需要 |
| `src/data/profile.json` | 关于页的头像路径与自我介绍段落 | 不需要（JSON 热更新） |
| `next.config.ts` | 框架级设置：图片格式、外部图片域名白名单 | **需要**（改完必须重启） |

**另外两个 JSON 是自动生成的，不要手改**（改了下次构建就被覆盖）：

| 文件 | 由什么生成 |
| --- | --- |
| `src/data/music.json` | 扫描 `public/audio/*.mp3` 的 ID3 标签 |
| `src/data/backgrounds.json` | 扫描 `public/images/backgrounds/` |

生成器是 `scripts/sync-media.mjs`，挂在 `predev` / `prebuild` 上自动跑。
想手动跑一次：`npm run sync:media`。

**一句话判断该改哪个文件：**

- 涉及「我是谁」（名字、简介、社交账号）→ `site.ts`
- 是按钮 / 标题 / 空状态这类界面装饰文字，换个说法意思不变 → `copy.ts`
- **换歌 / 换背景 → 不用改文件**，直接把 mp3 或图片丢进对应目录
- 是文章内容、个人经历 → `content/blog/*.md` 或 `data/profile.json`

### `theme.ts` 的字段

```ts
background: {
  imageOpacity: 0.55,   // 图片太抢眼就调低
  imageBlur: 0,
  fixed: true,          // 滚动时背景不动
  overlay: '...',       // 覆盖在图片上的遮罩，保证文字可读；首页不叠加这层
}

effects: { starfield: false, glowOrbs: false }   // 关掉氛围特效
```

### `site.ts` 的 `player` 段

```ts
player: {
  autoplay: true,              // 关掉就改成 false
  autoplayTrack: 'random',     // 'random' 每次随机；写数字 0 表示固定第一首
  defaultVolume: 0.5,          // 初始音量 0~1
}
```

---

## 3. `copy.ts` 的两种字段

打开 `src/config/copy.ts`，用 Ctrl+F 搜要改的中文原文（比如「最新文章」）就能定位。
里面有两种字段：

- **普通字符串**：`viewAllPosts: '全部文章'` —— 直接改引号里的字
- **函数**：``readingTime: (minutes) => `${minutes} 分钟` `` —— 文案里嵌了动态数字。
  改前后的文字就行，**别删掉 `${minutes}`**，删了数字就不显示了

分组一览：`common` / `header` / `search` / `home` / `blog` / `about` / `music` / `toc` / `theme` / `notFound`

---

## 4. 主题强调色系统（进阶）

顶栏那个色相滑块背后的机制，和上面的文案配置是两套独立系统，容易搞混：

1. `src/app/globals.css` 定义了一组 CSS 变量：`--accent-1/2/3`（渐变色标）、
   `--accent-strong-1/2`（按钮用的深色版）、`--accent-foreground`（按钮上文字颜色）、
   `--accent-shadow`（辉光）、`--accent-border`（卡片悬停描边）。默认全是白色系
2. 全站原本写死「粉色渐变」的地方（按钮、头像光环、卡片辉光、进度条）现在都引用这些变量，
   而不是直接写 `sakura-500` 这种固定颜色
3. `src/providers/ThemeProvider.tsx` 里的滑块是 0~100：拖到 0 是纯白，其他值用 HSL 现算，
   直接写成 `<html>` 的内联样式（优先级天然盖过 CSS 默认值），所有引用这些变量的地方瞬间跟着变
4. 选中的颜色存在浏览器 `localStorage`（key 是 `theme-hue`），刷新还在

**想改默认主题色** → 改 `globals.css` 里 `:root { --accent-1: #ffffff; ... }` 那一块。
**想换取色风格** → 改 `ThemeProvider.tsx` 的 `applyHue` 函数，里面是标准 HSL 转十六进制，
调饱和度 / 明度参数就能整体改调性。

> ⚠️ 以后新写界面时，凡是「强调色」性质的渐变或辉光，请用 `bg-accent-gradient`、
> `shadow-glow-accent` 这类工具类，**不要写死 `from-sakura-500 to-lavender-500`**，
> 否则那块颜色不会跟着滑块变。

---

## 5. 哪些东西是全自动的，不用配置

- **文章目录**：构建期从正文的 `#` / `##` / `###` 自动抽取
- **搜索索引**：构建期从全部文章自动生成，新增文章重新构建就自动收录
- **站点地图 sitemap**：随文章自动更新
- **阅读时长**：按中文 400 字/分、英文 200 词/分自动估算
- **歌单与专辑封面**：构建前自动扫描 `public/audio/`，从 ID3 读元信息并抽出内嵌封面
- **音频压缩**：码率高于 138k 的 mp3 自动压到 128k，内嵌封面与 ID3 原样保留
- **背景图列表**：构建前自动扫描 `public/images/backgrounds/`
- **背景图转码**：png/jpg 等自动转 WebP，宽度限制在 3840 以内
- **上下篇导航与相关文章**：按日期和标签自动计算

细节写在 `写作指南.md`。

> **两条转码依赖本机的 ffmpeg**（音频压缩、图片转 WebP）。
> Cloudflare 构建环境里没有 ffmpeg，检测不到会静默跳过，不会让构建失败 ——
> 所以**丢完媒体文件要在本地跑一次 `npm run dev` 或 `npm run sync:media` 再提交**，
> 否则进仓库的是没优化过的原文件。
>
> 原文件会挪到 `MioSrc/media-originals/` 留底，那个目录不进仓库，随时可以自己清空。

---

# 第二部分 · 发布

## 6. 发布流程

### 一分钟版

```bash
# 1. 在 content/blog/ 下写好 my-post.md

# 2. 本地自查，三条都必须零错误
npm run typecheck && npm run lint && npm run build

# 3. 提交推送
git add content/blog/my-post.md
git commit -m "post: 文章标题"
git push origin main

# 4. 等 Cloudflare 自动构建（约 2~3 分钟），然后刷新 https://reikaakane.com
```

推送到 `main` 会自动触发构建和部署，**不需要在 Cloudflare 控制台点任何东西**。

> **推不上去、报连接错误**？多半是代理没配给 git（浏览器能开 GitHub 不代表 git 能连）。见第 9.5 节。

### 为什么 `build` 不能省

很多错误只在构建期暴露，`npm run dev` 看不出来：

- frontmatter 字段写错或缺失 —— zod 校验在构建期直接报错
- MDX 语法错误（比如没闭合的 JSX 标签）
- 数学公式 KaTeX 语法错误

本地花 1 分钟，比推上去等 3 分钟再看 CI 报错划算得多。

> ⚠️ **但要知道 `build` 通过 ≠ 页面正常。** 有两类问题构建期完全静默：
> 图片域名不在 `remotePatterns` 白名单里、`cover` 写成了 Markdown 语法。
> 这两种都会构建成功、部署成功，然后线上图裂。见 `写作指南.md` 第三节。

### Cloudflare 自动构建链路

```
GitHub push (main)
  └─ Cloudflare Workers Builds
       ├─ npm clean-install              装依赖（840 多个包）
       ├─ npm run cf:build               = opennextjs-cloudflare build
       │    ├─ next build                文章在这一步静态生成
       │    └─ OpenNext 打包             产出 .open-next/worker.js
       └─ npx wrangler deploy            上传静态资源 + 部署 Worker
```

全程约 2~3 分钟。控制台 **Workers & Pages → mio-blog → Deployments** 能看实时日志。

---

## 7. Cloudflare 部署配置一览

Cloudflare 控制台 → Workers & Pages → `mio-blog` → Settings：

| 项 | 值 | 备注 |
|---|---|---|
| 项目名称 | `mio-blog` | 必须与 `wrangler.jsonc` 里的 `name` 完全一致 |
| **构建命令** | **`npm run cf:build`** | **改成 `npm run build` 会部署失败**，见第 9.1 节 |
| 部署命令 | `npx wrangler deploy` | 不用动 |
| 非生产分支部署命令 | `npx wrangler versions upload` | 只上传版本、不切流量 |
| 构建变量 | `NEXT_PUBLIC_SITE_URL` = `https://reikaakane.com` | **必须是构建变量**，不是运行时 Variables |
| 自定义域名 | `reikaakane.com`（根域） | Domains & Routes |
| Worker 绑定 | `ASSETS` 和 `IMAGES` | 见 `wrangler.jsonc` |

仓库里与部署相关的文件（**别删、别改名**）：

| 文件 | 作用 |
|---|---|
| `wrangler.jsonc` | Worker 名字、入口、绑定（`ASSETS` / `IMAGES`） |
| `open-next.config.ts` | OpenNext 适配配置，关键是 `incrementalCache` |

**`IMAGES` 绑定是干什么的**：线上的图片优化走它。页面渲染出的
`/_next/image?url=...` 地址由它处理——去取原图、缩放、转 AVIF。少了这个绑定全站图片会挂。
注意它和本地 `npm run dev` 用的 Node + sharp **不是同一套实现**。

---

## 8. 换域名的完整清单

**必须同时改两个地方，缺一不可：**

1. Domains & Routes 里的自定义域名
2. **构建变量 `NEXT_PUBLIC_SITE_URL`**，改完还要**重新触发一次构建**
   （改变量不会自动重建；随便推一个提交，或在 Deployments 点 Retry）

**为什么第 2 步必须做**：带 `NEXT_PUBLIC_` 前缀的值在 `next build` 时就被内联进产物，
构建结束后再改运行时变量不会生效。

**只改第 1 个的后果（2026-08-09 实际发生过）**：网站能正常打开，用户完全无感，
但 `sitemap.xml` 的 `<loc>`、`robots.txt` 的 `Sitemap:` 行、页面的 `og:url`
全都还指着已经不解析的旧域名。搜索引擎抓 sitemap 会全军覆没，分享卡片链接点不开。
**属于 SEO 层面的静默故障，只能靠主动自检发现。**

验证：

```bash
curl -s --noproxy '*' https://reikaakane.com/robots.txt | grep Sitemap
```

应该输出 `Sitemap: https://reikaakane.com/sitemap.xml`。

> **域名变更史**：最初绑的是子域 `blog.reikaakane.com`，2026-08-09 改为根域
> `reikaakane.com`，子域的 DNS 记录已删除（权威 NS 返回 NXDOMAIN）。`www` 子域始终未绑定。

---

# 第三部分 · 排查

## 9. 出问题怎么查

先去 Deployments 看日志，然后按下面的特征对号入座。**每一条都是实际踩过的。**

> 如果 Deployments 里**根本没有新构建**，那问题在推送之前，直接看 9.5。

### 9.1 `npm error Missing script: "cf:build"`

**原因**：`package.json` 里没有这个脚本 —— 通常是代码还没合并到 `main`，CI 拉到的是旧提交。

**确认**：看日志开头的 `added NNN packages`。正常是 **840 多个**；如果是 **561**，
那是引入 Cloudflare 适配层之前的依赖数量，说明 CI 拉到的是旧提交。

**解法**：确认 `git push` 成功、`origin/main` 确实包含你的提交：

```bash
git fetch origin && git log --oneline -1 origin/main
```

### 9.2 `Your Worker exceeded the size limit of 3 MiB. [code: 10027]`

**原因**：Worker 脚本体积超限。看日志里 `Total Upload: ... / gzip: ...` 那一行。

**常见诱因**：给 `src/lib/shiki.ts` 加了太多语言，或引入了体积大的运行时依赖。

**解法**：错误信息里会列出 `the 5 largest dependencies`，照着找。语言加多了就删掉不常用的。

### 9.3 文章全部 404 / 搜索结果为空 / sitemap 里没有文章

**症状**：首页和 `/blog` 能打开但列表空白，`/blog/<slug>` 返回 404，
`/search-index.json` 返回 `[]`。

**原因**：`open-next.config.ts` 里的 `incrementalCache` 没配或配错。

**构建期预渲染出来的页面是存在 incremental cache 里、并从这里取的**，不只是给 ISR 用的。
不配的话每次请求都是 cache miss，Worker 会当场重新执行路由代码 ——
而 Cloudflare Workers **没有文件系统**，`src/lib/posts.ts` 的 `fs` 一篇文章都读不到。

**怎么确认**：打开 `https://reikaakane.com/sitemap.xml`，如果 `lastmod` 是你
**当前访问的时刻**而不是构建时刻，就说明在实时渲染。连续刷几次看它变不变更可靠。

**正确配置**：

```ts
import { defineCloudflareConfig } from '@opennextjs/cloudflare';
import incrementalCache from '@opennextjs/cloudflare/overrides/incremental-cache/static-assets-incremental-cache';

export default defineCloudflareConfig({ incrementalCache });
```

### 9.4 `Service binding 'WORKER_SELF_REFERENCE' references Worker 'xxx' which was not found [code: 10143]`

**原因**：仓库里缺 `wrangler.jsonc`，CI 现场自动生成了一份，而它取的 Worker 名字是
`package.json` 的 `name`（`personal-web`），跟 Cloudflare 上的 `mio-blog` 对不上。

**解法**：确认 `wrangler.jsonc` 在仓库里、且 `name` 是 `mio-blog`。
（`package.json` 的 `name` 仍然是 `personal-web`，两者本来就不同名，别去「统一」它们。）

### 9.5 `git push` / `git pull` 报连接错误（推不上去，构建根本不会触发）

**症状**：`git push origin main` 卡二十几秒后失败：

```
fatal: unable to access 'https://github.com/TsukinoMio/Mio-blog.git/':
Failed to connect to github.com:443 after 21094 ms: Could not connect to server
```

**原因**（2026-08-09 实际排查过）：**Clash 的「系统代理」开关只写进 Windows 的
WinINET 注册表**。浏览器会读这份设置，但 **Git for Windows 自带的 curl 不读它** ——
git 只认两样东西：

1. git 配置里的 `http.proxy` / `https.proxy`
2. 环境变量 `HTTP_PROXY` / `HTTPS_PROXY`

当时这两样都是空的，所以 git 在裸连 github.com，直连打不通就超时。
**表现上非常迷惑：浏览器能开 GitHub 网页，git 却连不上，看起来像「网络时好时坏」，
其实是稳定失败。**

**解法**（已配置，写进全局 `~/.gitconfig`，所有仓库通用）：

```bash
git config --global http.proxy http://127.0.0.1:7897
```

```bash
git config --global https.proxy http://127.0.0.1:7897
```

> `7897` 是 Clash Verge 的默认混合端口。改过端口的话以 Clash 界面显示的为准。

**副作用 —— 关掉 Clash 之后 git 会反过来连不上**，因为它还在往一个没人监听的本地端口发请求：

```
fatal: unable to access '...': Failed to connect to 127.0.0.1 port 7897: Connection refused
```

**怎么清掉**（不用代理时执行，两条都要）：

```bash
git config --global --unset-all http.proxy
```

```bash
git config --global --unset-all https.proxy
```

**怎么查当前状态**（有输出＝配着，无输出＝已清掉）：

```bash
git config --global --get-regexp proxy
```

**临时用一次而不改全局配置**：

```bash
git -c http.proxy=http://127.0.0.1:7897 push origin main
```

> 顺带记一笔：`~/.gitconfig` 里那条 `http.version = HTTP/1.1` 是更早之前
> 撞上 HTTP/2 报错时加的绕过配置，与代理无关，留着无害。

### 9.6 页面正常但图片全裂

**原因 A**：图片域名不在 `next.config.ts` 的 `images.remotePatterns` 白名单里，
`/_next/image` 返回 400。

**原因 B**：`cover` 写成了 Markdown 图片语法 `![alt](url)` 而不是纯地址。

两种都**在构建期毫无征兆**。详见 `写作指南.md` 第三节。

---

## 10. 上线后自检

```bash
curl -s -o /dev/null -w "%{http_code}\n" --noproxy '*' https://reikaakane.com/blog/physically-based-rendering
```

应该返回 `200`。再核对这三个：

- `/sitemap.xml` —— 条目数应该是 `3 + 文章数`；`<loc>` 的域名要是**当前域名**；
  `lastmod` 应该是构建时刻而不是访问时刻
- `/search-index.json` —— 不应该是 `[]`
- 随便打开一篇有配图的文章，**确认图真的显示出来了**（构建通过不代表图没裂）

> **`--noproxy '*'` 不能省**：实测经 Clash 访问本站会 TLS 握手失败
> （curl 退出码 35 / `schannel: failed to receive handshake`），而同一个代理访问
> github.com 是正常的。不绕过代理会把代理问题误判成网站挂了。
> 注意这跟 git 的要求**正好相反**（git 必须走代理），别搞混。

> **刚推完别急着判死刑**：部署切换有几十秒的过渡期，不同边缘节点切换时间不一致。
> 实测出现过「sitemap 已经是新的，但同一秒请求文章页还是旧节点返回 404」的情况。
> 看到异常先隔十几秒重试一次再下结论。

---

## 11. 回滚

线上出问题想快速退回上一个版本：

```bash
git revert HEAD          # 撤销最近一次提交（会生成新提交，不改写历史）
git push origin main     # 推上去自动触发重新构建
```

需要退回更早的版本就 `git log --oneline` 找到目标提交，`git revert <hash>`。

Cloudflare 的 Deployments 页面也保留了历史版本，可以直接回滚到之前的部署 ——
但那样仓库代码和线上会不一致，之后任何一次 push 都会把它覆盖掉。
**优先用 `git revert`**，保持仓库和线上一致。

---

## 12. 本地预览 Cloudflare 产物（可选）

```bash
npm run cf:preview
```

它会完整走一遍 OpenNext 打包，在本地用 workerd 跑起来，比 `npm run dev` 更接近线上真实环境。

**Windows 上需要先开启开发者模式**（设置 → 系统 → 开发者选项 → 开发人员模式）。
OpenNext 打包时要创建符号链接，没开的话会报：

```
Error: EPERM: operation not permitted, symlink ...
```

> **当前站主机器没开这个**，所以 OpenNext 最终产物、gzip 体积、
> Workers 侧的图片优化行为都**无法在本地验证**，只能靠 CI 日志或线上实测。
> AI 助手报告验证结果时应当明确区分「本地验过」和「只能上线后确认」。

日常写文章用 `npm run dev` 就够，这个只在怀疑「本地好好的、线上不对」时才需要。
