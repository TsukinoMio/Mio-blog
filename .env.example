# 复制成 .env.local 使用（.env.local 已经在 .gitignore 里，不会被提交）
# 这两个都不是必填 —— 不设也能正常跑，只是会退回下面注释里说的默认值

# 线上域名。影响 sitemap.xml 里的链接和分享卡片的图片地址。
# 不设的话退回 http://localhost:3000
# NEXT_PUBLIC_SITE_URL=https://your-domain.com

# 音频文件的 CDN 前缀。设了之后 src/data/music.json 里所有以 / 开头的
# 相对路径都会自动加上它（写成完整 https:// 开头的地址则原样不动）。
# 不设的话直接用 public/audio/ 下的本地文件
# NEXT_PUBLIC_AUDIO_BASE_URL=https://cdn.your-domain.com
