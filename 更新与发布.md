# 更新博客：从改内容到线上生效

> **四份文档的分工**（内容不重复）：
> - **本文件** —— 更新与发布：改完东西怎么让线上生效、部署配置、出问题怎么查
> - **`README.md`** —— 操作手册：怎么写文章、换背景、换音乐、各功能怎么工作
> - **`MioSrc/配置系统说明.md`** —— 速查表：我想改 XX，去哪个文件
> - **`AI_CONTEXT.md`** —— 给 AI 助手看的项目交接文档

---

## 一分钟版

最常见的场景是加一篇文章：

```bash
# 1. 在 content/blog/ 下写好 my-post.mdx（frontmatter 模板见 README）

# 2. 本地自查，三条都必须零错误
npm run typecheck && npm run lint && npm run build

# 3. 提交推送
git add content/blog/my-post.mdx
git commit -m "post: 文章标题"
git push origin main

# 4. 等 Cloudflare 自动构建（约 3 分钟），然后刷新 https://blog.reikaakane.com
```

推送到 `main` 会自动触发构建和部署，**不需要在 Cloudflare 控制台点任何东西**。

---

## 1. 改什么，去哪改

| 我想做的事 | 改这里 | 详细说明 |
|---|---|---|
| 加/改文章 | `content/blog/*.mdx` | README「写一篇新文章」 |
| 加文章配图 | `public/images/blog/<slug>/` | README「从其他笔记软件导入」 |
| 换背景图 | `public/images/backgrounds/` + `src/config/theme.ts` | README「换背景图」 |
| 换音乐 | `public/audio/` + `src/data/music.json` | README「换音乐」 |
| 改界面文字 | `src/config/copy.ts` | README「改文案与站点信息」 |
| 改站名/标语/社交链接 | `src/config/site.ts` | 同上 |
| 改自我介绍/头像 | `src/data/profile.json` | README「改关于页」 |
| **代码块用了新语言** | **`src/lib/shiki.ts`** | **见下面第 3 节，容易漏** |

列表页、目录、搜索索引、sitemap、相关文章**全部自动更新**，不需要任何额外配置。

---

## 2. 发布流程

### 2.1 本地自查（推之前一定要跑）

```bash
npm run typecheck   # TypeScript
npm run lint        # ESLint
npm run build       # 生产构建
```

**`build` 不能省。** 很多错误只在构建期才暴露，本地 `npm run dev` 看不出来：

- frontmatter 字段写错或缺失 —— `src/lib/schema.ts` 的 Zod 校验在构建期直接报错
- MDX 语法错误（比如没闭合的 JSX 标签）
- 数学公式 KaTeX 语法错误

这三条在本地花 1 分钟，比推上去等 3 分钟再看 CI 报错划算得多。

### 2.2 推送

```bash
git add <改动的文件>
git commit -m "说明"
git push origin main
```

仓库：`https://github.com/TsukinoMio/Mio-blog`，只用 `main` 一个分支。

### 2.3 Cloudflare 自动构建

推送后自动触发，链路是：

```
GitHub push (main)
  └─ Cloudflare Workers Builds
       ├─ npm clean-install              装依赖（840 多个包）
       ├─ npm run cf:build               = opennextjs-cloudflare build
       │    ├─ next build                文章在这一步静态生成
       │    └─ OpenNext 打包             产出 .open-next/worker.js
       └─ npx wrangler deploy            上传静态资源 + 部署 Worker
```

全程约 2~3 分钟。在 Cloudflare 控制台的 **Workers & Pages → mio-blog → Deployments** 能看到实时日志。

---

## 3. 写文章时的一个硬约束：代码块语言白名单

**这是最容易踩、而且不会报错的坑。**

`src/lib/shiki.ts` 里只注册了三种语言：

```ts
const LANGS = [glsl, tsx, bash];
```

用了清单外的语言（比如 ` ```python `），**构建不会失败，但那段代码不会有配色**，会当成纯文本渲染。没有任何警告。

### 为什么要有白名单

Shiki 的完整包含 200 多种语言语法，打进 Worker 后 gzip 约 1.74 MB。Cloudflare Workers 免费版的脚本体积上限是 **3 MiB（gzip 后）**，带上完整包会直接超限，部署报错：

```
Your Worker exceeded the size limit of 3 MiB. [code: 10027]
```

只保留用到的三种语言后，同一条依赖链从 10.15 MB / gzip 1.74 MB 降到 0.61 MB / gzip 0.11 MB。

### 加一种新语言

假设要写 Python，改 `src/lib/shiki.ts` 两行：

```ts
import bash from '@shikijs/langs/bash';
import glsl from '@shikijs/langs/glsl';
import python from '@shikijs/langs/python';   // ← 1. 加这行
import tsx from '@shikijs/langs/tsx';

// ...

const LANGS = [glsl, tsx, bash, python];      // ← 2. 加进数组
```

依赖语法会自动跟着走，不用手动补（比如 `glsl` 模块自己 export 的就是 `[...c, glsl]`）。

改完照常 `npm run build`，然后打开对应文章确认代码块有颜色。

> 常用语言的包名：`python` `json` `yaml` `sql` `rust` `go` `cpp` `csharp` `java` `css` `html` `markdown` `typescript` `javascript` `shellscript`。
> 完整列表见 `node_modules/@shikijs/langs/dist/` 下的文件名。

---

## 4. 部署配置一览

Cloudflare 控制台 → Workers & Pages → `mio-blog` → Settings：

| 项 | 值 | 备注 |
|---|---|---|
| 项目名称 | `mio-blog` | 必须与 `wrangler.jsonc` 里的 `name` 完全一致 |
| **构建命令** | **`npm run cf:build`** | **改成 `npm run build` 会部署失败**，见第 5 节 |
| 部署命令 | `npx wrangler deploy` | 不用动 |
| 非生产分支部署命令 | `npx wrangler versions upload` | 只上传版本、不切流量 |
| 构建变量 | `NEXT_PUBLIC_SITE_URL` = `https://blog.reikaakane.com` | **必须是构建变量**，不是运行时 Variables |
| 自定义域名 | `blog.reikaakane.com` | Domains & Routes |

仓库里与部署相关的文件（一般不需要动）：

| 文件 | 作用 |
|---|---|
| `wrangler.jsonc` | Worker 名字、入口、绑定（`ASSETS` / `IMAGES`） |
| `open-next.config.ts` | OpenNext 适配配置，关键是 `incrementalCache` |

**`NEXT_PUBLIC_SITE_URL` 为什么必须是构建变量**：带 `NEXT_PUBLIC_` 前缀的值在 `next build` 时就被内联进产物，构建结束后再改运行时变量不会生效。它影响 `sitemap.xml` 里的链接和分享卡片的地址。

---

## 5. 出问题怎么查

先去 Deployments 看日志，然后按下面的特征对号入座。这四个都是实际踩过的。

### 5.1 `npm error Missing script: "cf:build"`

**原因**：`package.json` 里没有这个脚本 —— 通常是代码还没合并到 `main`，CI 拉到的是旧提交。

**确认**：看日志开头的 `added NNN packages`。正常是 **840 多个**；如果是 **561**，那是引入 Cloudflare 适配层之前的依赖数量，说明 CI 拉到的是旧提交。

**解法**：确认 `git push` 成功、`origin/main` 确实包含你的提交：

```bash
git fetch origin && git log --oneline -1 origin/main
```

### 5.2 `Your Worker exceeded the size limit of 3 MiB. [code: 10027]`

**原因**：Worker 脚本体积超限。看日志里 `Total Upload: ... / gzip: ...` 那一行，gzip 值超过 3 MiB 就会挂。

**常见诱因**：给 `src/lib/shiki.ts` 加了太多语言，或引入了体积大的运行时依赖。

**解法**：错误信息里会列出 `the 5 largest dependencies`，照着找。语言加多了就删掉不常用的。

### 5.3 文章全部 404 / 搜索结果为空 / sitemap 里没有文章

**症状**：首页和 `/blog` 能打开但列表空白，`/blog/<slug>` 返回 404，`/search-index.json` 返回 `[]`。

**原因**：`open-next.config.ts` 里的 `incrementalCache` 没配或配错。

这一条值得展开：**构建期预渲染出来的页面是存在 incremental cache 里、并从这里取的**，不只是给 ISR 用的。不配的话每次请求都是 cache miss，Worker 会当场重新执行路由代码 —— 而 Cloudflare Workers **没有文件系统**，`src/lib/posts.ts` 的 `fs` 一篇文章都读不到。

**怎么确认是这个问题**：打开 `https://blog.reikaakane.com/sitemap.xml`，如果 `lastmod` 是你**当前访问的时刻**而不是构建时刻，就说明页面在实时渲染，没走预渲染产物。

**正确配置**：

```ts
import { defineCloudflareConfig } from '@opennextjs/cloudflare';
import incrementalCache from '@opennextjs/cloudflare/overrides/incremental-cache/static-assets-incremental-cache';

export default defineCloudflareConfig({ incrementalCache });
```

### 5.4 `Service binding 'WORKER_SELF_REFERENCE' references Worker 'xxx' which was not found [code: 10143]`

**原因**：仓库里缺 `wrangler.jsonc`，CI 现场自动生成了一份，而它取的 Worker 名字是 `package.json` 的 `name`（`personal-web`），跟 Cloudflare 上的 `mio-blog` 对不上。

**解法**：确认 `wrangler.jsonc` 在仓库里、且 `name` 是 `mio-blog`。

### 5.5 上线后自检

```bash
curl -s -o /dev/null -w "%{http_code}\n" https://blog.reikaakane.com/blog/physically-based-rendering
```

应该返回 `200`。再核对这两个：

- `https://blog.reikaakane.com/sitemap.xml` —— 条目数应该是 `3 + 文章数`
- `https://blog.reikaakane.com/search-index.json` —— 不应该是 `[]`

---

## 6. 本地预览 Cloudflare 产物（可选）

```bash
npm run cf:preview
```

它会完整走一遍 OpenNext 打包，在本地用 workerd 跑起来，比 `npm run dev` 更接近线上真实环境。

**Windows 上需要先开启开发者模式**（设置 → 系统 → 开发者选项 → 开发人员模式）。OpenNext 打包时要创建符号链接，没开的话会报：

```
Error: EPERM: operation not permitted, symlink ...
```

日常写文章用 `npm run dev` 就够，这个只在怀疑「本地好好的、线上不对」时才需要。

---

## 7. 回滚

线上出问题想快速退回上一个版本：

```bash
git revert HEAD          # 撤销最近一次提交（会生成一个新提交，不改写历史）
git push origin main     # 推上去自动触发重新构建
```

需要退回更早的版本就 `git log --oneline` 找到目标提交，`git revert <hash>`。

Cloudflare 的 Deployments 页面也保留了历史版本，可以直接回滚到之前的部署 —— 但那样仓库代码和线上会不一致，之后任何一次 push 都会把它覆盖掉。**优先用 `git revert`**，保持仓库和线上一致。
